version: '3.8'

services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: student
      POSTGRES_DB: nutrition_db
    ports:
      - "5436:5432" # <-- Ваш новый произвольный порт